# Radhakrishnan Balakrishnan
* Refer the documentations `API_Spec` and `CommandLine_Program_Spec` for downloading and running the REST API and the Standalone(command line) program respectively

# talking-clock
